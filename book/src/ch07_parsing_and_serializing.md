# Parsing and Serializing

(coming soon)

<!--
TODO describe the different parsers and serializers available in Sophia (mentioning the feature gates)
Start with Turtle, then N-Triples, then TriG, then N-Quads, then RDF/XML, then JSON-LD.
For Turtle, give several examples or parsing (from string, from Read) and serializing (to Write, stringify),
and note that the same patterns exist for all parsers/serializers.
Then more briefly give examples for the other syntaxes.
-->


<!--
TODO then add section on using builder or options to customize behaviour,
like adding a base to a parser or requiring to preserve bnode labels
(with a note on why this is not the default);
or setting serialization to pretty or canonical.
(note that those options depend on the syntax, because they do not always make sense)
-->

